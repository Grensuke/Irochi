# ==================================================
# Irochi Backend — Python Dependencies (Dummy Phase)
# ==================================================
# Pinned versions for reproducibility.
# Only dependencies needed for the dummy/scaffold
# backend are included. Production dependencies
# (redis, asyncpg, confluent-kafka, scikit-learn,
# xgboost, river, etc.) will be added when the real
# pipeline is implemented.
# ==================================================

# --- Core ---
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.3

# --- Persistence ---
sqlalchemy==2.0.38
asyncpg==0.30.0
alembic==1.14.1

# --- Testing ---
pytest==8.3.5
pytest-asyncio==0.25.3
httpx==0.28.1

# --- WebSocket ---
websockets==15.0.1

# --- Streaming Foundation (WP-C) ---
aiokafka==0.12.0
redis==5.2.1
